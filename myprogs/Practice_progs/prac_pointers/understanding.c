/*
int a =5;
int *ptr = &a;
printf("%d %p %x\n", a,ptr,ptr);
*/

/*
Multiple variables can point towards the same memory locn without interfering with each other but each affecting memory location equally.
Right -> Left Associativity.

*/

(*ptr).a is same as ptr->a